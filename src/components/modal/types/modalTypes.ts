export interface SectionModal {
	name: string
}
export interface ModalTypes {
	perfumDetailsModal: SectionModal
}

export const modalTypes: ModalTypes = {
	perfumDetailsModal: { name: "perfumDetailsModal" },
}
